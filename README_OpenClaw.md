## GitHub Issue Summarizer (OpenClaw)

### Requirements
- `gh` CLI installed + authenticated
- `openclaw` CLI running (`openclaw gateway status`)
- WhatsApp channel connected

### Run manually
```bash
./scripts/summarize_new_issues.sh

